## 1. Robinhood Exact Caching Protocol

Whenever initializing a new task or instructed to load context, you MUST read the Memory Bank files in this EXACT strict order:
1. `projects/robinhood/memory-bank/systemArchitecture.md` (Static)
2. `projects/robinhood/memory-bank/techContext.md` (Static)
3. `projects/robinhood/memory-bank/progress.md` (Low-frequency updates)
4. `projects/robinhood/memory-bank/activeContext.md` (High-frequency updates)

## 2. Cognitive Blackbox & Boundary Isolation Protocol (认知黑盒与边界隔离)

### 2.1 Phase Boundary Enforcement (阶段边界强制)

本项目按 Phase 组织开发。每个 Phase 由独立的 Python 模块实现（`src/phaseN_<name>.py`）。在开发任何一个 Phase N 时，AI 必须遵守以下只读边界：

- **可读取范围**：
  - `projects/robinhood/memory-bank/` 下所有文件（架构文档、决策日志、进度记录）
  - `projects/robinhood/src/` 下所有 `*.py` 文件的 **docstring 和函数签名**（仅接口层）
  - `projects/robinhood/src/` 下当前 Phase N 对应的 `phaseN_*.py` 文件 **完整源码**
  - `projects/robinhood/config/` 下所有配置文件
  - `projects/robinhood/tests/` 下当前 Phase N 对应的测试文件

- **严禁读取范围**：
  - **严禁**读取任何非当前 Phase 的 `phaseM_*.py`（M ≠ N）的 **函数体实现代码**。
  - **严禁**读取非当前 Phase 测试文件中的 **测试逻辑细节**。
  - **严禁**读取 `projects/robinhood/data/` 中的任何运行时数据文件（除非当前 Phase 明确需要）。

- **例外**：如需了解其他 Phase 模块的输出格式，应读取 `memory-bank/systemArchitecture.md` 中的接口定义（JSON Schema），而非模块源码。

### 2.2 Import-Level Isolation (导入级隔离)

- 当前 Phase 的模块 `import` 其他 Phase 模块时，仅允许 **接口级导入**（即 `from phaseN_module import function_signature`），不得在被导入模块中执行副作用操作。
- 每个 Phase 的模块必须提供 `__all__` 列表，显式声明公开接口。
- 违反此规则：开发成果不计入产出，需回到 Step 0 侦察阶段重新开始。

### 2.3 Context Token Budget per Phase (Phase 级 Token 预算)

- 在开发一个 Phase 期间，与该 Phase 无关的源码阅读 Token 占用不得超过总上下文 Token 的 15%。
- 超过红线时，The Archivist 应立即执行上下文提纯（按根宪法铁律五执行），删除无关源码片段。

### 2.4 Blackbox Contract Enforcement (黑盒契约强制)

- 所有跨 Phase 通信必须通过 `memory-bank/systemArchitecture.md` 中定义的 JSON Schema 契约。不得绕过契约直接访问对方模块的内部数据结构。
- 如需修改跨 Phase 契约（如在 systemArchitecture.md 中的 JSON Schema），必须先发起 CRP（Consultation Request Pack），经 PM 批准后方可执行。

## 3. DeepSeek API Interaction Constraints (DeepSeek API 交互约束)

### 3.1 Single Model Mandate (单模型强制)

- 本项目的 LLM 后端为 **DeepSeek API（唯一指定）**。不得引入任何其他模型提供商的 API 调用。
- `deepseek_client.py` 是本项目所有 LLM 调用的唯一入口。任何模块不得绕过该入口直接调用 DeepSeek HTTP API。

### 3.2 Prompt Construction Rules (Prompt 构建规则)

- 所有发送给 DeepSeek 的 System Prompt 必须包含以下三要素（按顺序）：
  1. **角色声明**：`"You are a senior quantitative analyst working on a proprietary financial analysis system."`
  2. **输出格式契约**：`"You MUST output valid JSON matching the following schema. Do NOT wrap your response in markdown code fences."`
  3. **思维链引导**（见根宪法 §6.4.1）
- System Prompt 中 **禁止** 包含任何可能被模型误解为对话格式的 Markdown 语法（如 `[text](url)` 自动链接）。

### 3.3 Response Sanitization Pipeline (响应净化管道)

所有 DeepSeek API 响应在进入业务逻辑前，必须经过以下净化管道（在 `deepseek_client.py` 中实现）：
1. **Markdown fence stripping**: 去除 ```json ... ``` 包裹
2. **Auto-link unwrapping**: 脱壳 `[path](url)` 自动链接格式
3. **JSON parse with regex fallback**: 直接解析 → 失败 → regex 提取
4. **ASCII sanitization**: `clean_ascii_only()` 全字段递归清洗

### 3.4 Financial Reasoning Guardrails (金融推理护栏)

- Temperature 和 Top-p 参数由 `deepseek_client.py` 统一管理，默认值见项目配置文件。
- 金融推演类调用 **必须** 使用低 Temperature（≤0.3），减少随机性对推理一致性的影响。
- 交易决策类调用 **必须** 请求模型的 Thinking 输出（`reasoning_content`），并在 Markdown 报告中呈现该推理链。

## 4. Anti-Overfitting Enforcement (反拟合强制)

继承自 `systemArchitecture.md` 三大天条：
1. **UI 纪律**：所有输出 ASCII-only，`output_formatter.py` 硬校验。
2. **物理隔离**：`deepseek_client.py` 只做 LLM 调用，不发送任何交易指令。
3. **反拟合**：Temperature 不得 >0.5；信号生成必须 ≥3/4 维度共振；禁止参数暴力穷举；必须包含空仓压力测试。