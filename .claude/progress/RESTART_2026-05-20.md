# 重启指南 — 2026-05-20

**首先读这个。§0 检查通过前不要写代码。**

---

## 0. 启动检查

SessionStart 链自动运行（5 个 hook），预期输出：

```
[integrity_check] OK: 13 hooks verified, settings intact, state dir present
[config_guardian] ... all guards passed
[time_anchor] OK: ...
[task_manifest] OK: workflow rules injected
[ startup_report ] 启动健康报告
```

上次会话 emergency_override 了 5 个 PICA gate（工作流强制执行系统初始实现），SessionStart 差异检测会提醒补做。

### 0.1 快速验证

```bash
claude mcp list                    # 3 connected (context7, github, chrome-devtools)
git log --oneline -3               # 确认最新提交
```

---

## 1. 当前状态

**管线**: 10/10 跑通，1302 测试 0 失败
**工作流强制执行**: 9 hook × 5 事件，完整运行
**路线图**: `E:/AI_Studio_Workspace\PROJECT_ROADMAP.md`

---

## 2. 今日任务（按优先级）

| # | 任务 | 类型 | 预计耗时 |
|:---:|------|------|:---:|
| **1** | 影子敢死队 #4 重设计讨论 | 设计讨论 | 30 分钟 |
| **2** | 影子类型最终确认（16E+8D+5机制） | 审批 | 15 分钟 |
| **3** | 政治情绪追踪 — 从归档恢复 + 集成 | 功能 | 1-2 小时 |
| **4** | Mock 管线修复 — `--mock` 标志传到 HTTP 抓取器 | Bug 修复 | 30 分钟 |

任务 1-2 必须先做（阻塞影子系统实施）。
任务 3-4 可并行或延后。

---

## 3. 昨日完成

- 工作流强制执行系统（integrity_check + task_manifest + danger_guard + stop_gate_check + Guard 5）
- CLAUDE.md 标签（45 条规则）
- Skill 使用追踪（skill_profiler + skill_report）
- 夹具 Phase 2（8 个夹具）
- 项目路线图
- 红方审计 9 项发现全部修复

---

## 4. 工作流提醒

开始任务前声明类型到 `E:\AI_Studio_Workspace\.claude\state\current_task.json`。
Stop gate 会在会话结束时校验 PICA 产物——git diff 交叉验证，声明 "explore" 却写代码会被拦截。

详见 `task_manifest.py` 注入的规则。

---

## 5. 不可触碰

- ❌ 影子投票机制（已永久移除）
- ❌ `flash_preprocessor.py`（已被 `flash_triage.py` 替代）
- ❌ ELITE Gate 2 觉醒（用户延后）
- ❌ Phase I 激活（需要预测历史数据）
- ❌ 日本/OPEC 新闻源（已确认不可用）

---

**更新**: 2026-05-19 会话结束
